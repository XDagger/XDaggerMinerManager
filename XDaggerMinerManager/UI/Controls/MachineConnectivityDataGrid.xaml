<UserControl x:Class="XDaggerMinerManager.UI.Controls.MachineConnectionDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XDaggerMinerManager.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="dataGrid" Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="机器名称" IsReadOnly="True" Binding="{Binding FullName, Mode=OneWay}" />
                <DataGridTextColumn Header="IP地址" IsReadOnly="True" Binding="{Binding IpAddressV4, Mode=OneWay}" />
                <DataGridTextColumn Header="PING连接结果" IsReadOnly="True" Binding="{Binding CanPing, Mode=OneWay}" />
                <DataGridTextColumn Header="远程路径连接结果" IsReadOnly="True" Binding="{Binding CanRemotePathAccess, Mode=OneWay}" />
                <DataGridTextColumn Header="RemotePowershell连接结果" IsReadOnly="True" Binding="{Binding CanRemotePowershell, Mode=OneWay}" />

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
